import { AssetEnrichCapabilityFilterPipe } from './asset-enrich-capability-filter.pipe';

describe('AssetEnrichCapabilityFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AssetEnrichCapabilityFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
